# Employee-Management-System
Full-stack Expense Tracker built with Spring Boot 3.2, Java 21, and MySQL. Features a REST API for managing Expenses and Categories with full CRUD operations, Swagger UI for API testing, and a clean Vanilla JS frontend served directly from Spring Boot — no separate frontend server required.
